// Example of a Promises
// function fetchDataPromise() {
//   return new Promise((resolve, reject) => {
//     setTimeout(() => {
//       resolve("Data loaded");
//     }, 1000);
//   });
// }
// fetchDataPromise().then((data) => console.log(data));

// code runs after nine seconds
// setTimeout(() => alert("run after 9 seconds"), 9000);

// this runs after 4 seconds
// setInterval(() => alert("counting... after 4 seconds"), 4000);
